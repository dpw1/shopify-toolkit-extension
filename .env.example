# Optional: override the bundled App Store catalog URL (see src/config/appsCatalog.defaults.json).
# VITE_APPS_CATALOG_JSON_URL=

# Optional: override themes.json URL for the Theme tab (see themesCatalogJsonUrl in appsCatalog.defaults.json).
# VITE_THEMES_CATALOG_JSON_URL=
